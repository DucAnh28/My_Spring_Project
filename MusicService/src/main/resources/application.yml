server:
  port: 2000

spring:
  application:
    name: music
    main:
      allow-bean-definition-overriding: true
  datasource:
    #    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?currentSchema=music_app
    #    username: ${DB_USERNAME}
    #    password: ${DB_PASSWORD}
    #    driver-class-name: 'org.postgresql.Driver'
    url: jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:5432/music
    username: postgres.dwurmpcxhclqjnsyngij
    password: MySpringProject
    driver-class-name: 'org.postgresql.Driver'
    hikari:
      maximumPoolSize: 120
      idleTimeout: 300000
      connectionTimeout: 300000
      leakDetectionThreshold: 300000
      schema: ${spring.application.name}
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        generate_statistics: false
        dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        time_zone: Asia/Ho_Chi_Minh
    open-in-view: false
#  jwt:
#    time-out: ${JWT_SECRET_TIME}
#    key: ${JWT_SECRET_KEY}
